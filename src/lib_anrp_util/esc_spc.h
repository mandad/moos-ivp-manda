#ifndef __esc_spc
#define __esc_spc

#include <string>
#include "bom.h"

using namespace std;

string esc_spc(string);
string reverse_esc_spc(string);

bom esc_spc(bom);
bom reverse_esc_spc(bom);

#endif
